TODO LIST :

- Tab-based label views + rn it does what is shouldn't, it should do what the prefs. expect it to
	if the prefs say for anything to "always" or "tab to toggle" and the user toggles it, it should be constant on (no need for also hover it)

make everything inside costumize a dragging stuff, so the displays pozition and the resize drag, not click then click again (ofc at the first time when the user clicks if on the left sidebar it should stay at cursor until the user "places" - clicks it somewhere)

FUTURE PLANS :

- full dirty-tracking / partial re-render (React-style reconciliation): would
	mean per-component change detection + keyed diffing + invalidation
	plumbed through every mutation path.

if i create e new chip (ctrl n) and i use save on it (ctrl s) it show us as the "save as | rename" not as "save" making it not starred if i do rename - sine i obv. don't want to save my custom chip as "New Chip <n>"

make sure opening a new chip (ctrl+n) always opens "New Chip" if it is not in the library already

going into edit mode should make the entire wire network in edit mode, so everything that the wire is connected to

separate the rendering from logic (rn lagging logic laggs the ui too)

add components or some form of input that can run rust code (like toggling a setting with your own circuits)

nicer documentation : in game features, keybinds, maybe some educational stuff like how to use x-y, or maybe to help make complex circuits (or atleast links to real help)
