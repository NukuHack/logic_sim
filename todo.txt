TODO LIST:

- Tab-based label views + rn it does what is shouldn't, it should do what the prefs. expect it to
	if the prefs say for anything to "always" or "tab to toggle" and the user toggles it, it should be constant on (no need for also hover it)

make everything inside costumize a dragging stuff, so the displays pozition and the resize drag, not click then click again (ofc at the first time when the user clicks if on the left sidebar it should stay at cursor until the user "places" - clicks it somewhere)

RENDER PERF (scene rebuilds every frame -- fine to keep, but cut incidental allocation) :

- ChipLibrary::get -> store chips as Arc<ChipDescription> internally so any
	remaining "need an owned copy to dodge the borrow checker" spot becomes a
	pointer clone instead of a deep Vec-of-Vecs clone. Low risk, fixes this
	class of bug everywhere at once instead of one call site at a time.

- place_sub_chips (render/scene/placed.rs) is the biggest remaining cost:
	per subchip, per frame it currently allocates input_bits/output_bits Vecs,
	recomputes calculate_min_chip_size/calculate_default_pin_layout from font
	metrics, and clones label/pin_colour_info/internal_data.
	  a) cache size + pin-y layout per chip TYPE (keyed by chip name),
	     invalidated only when that chip's own definition changes -- it does
	     not depend on per-instance placement, so recomputing it every frame
	     for every instance is pure waste.
	  b) make PlacedSubChip borrow label/pin_colour_info/internal_data
	     (&'a Option<String>, &'a [(i32,Color)], &'a [u32]) instead of owning
	     clones, same lifetime story it already uses for `desc: &'a ChipDescription`.
	  c) pass place_sub_chips a reusable `&mut Vec<PlacedSubChip>` (clear +
	     refill) instead of returning a freshly allocated Vec each call.

- capture_pin_states / capture_internal_states build a fresh heap-allocated
	Vec<i32> id-path per pin/chip as a HashMap key (clone + push + push).
	Only matters if these run on the per-frame render path rather than just
	on rebuild-after-edit -- if so, replace the Vec<i32> key with a packed
	numeric key (u64, bit-packed path+id+is_input) to drop both the
	allocation and the Vec-hashing cost.

FUTURE PLANS (can be ignored now) :

- full dirty-tracking / partial re-render (React-style reconciliation): would
	mean per-component change detection + keyed diffing + invalidation
	plumbed through every mutation path. Bigger commitment than the project
	currently wants; only worth it if A-D above still leave frame time short.

if i create e new chip (ctrl n) and i use save on it (ctrl s) it show us as the "save as | rename" not as "save" making it not starred if i do rename - sine i obv. don't want to save my custom chip as "New Chip <n>"

make sure opening a new chip (ctrl+n) always opens "New Chip" if it is not in the library already

going into edit mode should make the entire wire network in edit mode, so everything that the wire is connected to

separate the rendering from logic (rn lagging logic laggs the ui too)

speedup : 
add a checkbox into the customization menu (default off) like "make lut"
add an optional lut in every circuit (only in memory, so no serialization), and in simulation just do an "is_some() then use lookup" - important use key, and values as more compressed variables
	rn even one bit of value is 16 bit (so it's a really big lut uselessly) - example : most of the circuits don't care about the input being "off or disabled" so if possible minimize the lut
add a pattern maching or something like that to the customization menu, that would make it so some pre-defined lookup tables mach the lut generated lut by the circuit
	so it would use real math in the code instead of lut : like 8 bit adder would be 2x8+1 bit in, 8+1 bit out so a lot, but addition with pure math is insainly fast

add components or some form of input that can run rust code (like toggling a setting with your own circuits)

nicer documentation : in game features, keybinds, maybe some educational stuff like how to use x-y, or maybe to help make complex circuits (or atleast links to real help)

maybe a custom "View" for a nand with transistors, grounding and such (just 4 fun)